# Package `0.4.0-acceptance.10`

Objetivo: diagnóstico final raw DOM × shortcode-unwrapped DOM × parser emitted dos 5 resíduos estruturais remanescentes, sem alterar Knowledge Document `2.0.1`, parser, hashes, readiness ou gate.

Artefato: `base-conhecimento-inteligencia-integrada-0.4.0-acceptance.10.zip`

SHA-256: `2df68c41039bc4ba4f950678c4153a0ce70b0b5d3c135ecf746ebab3d61b2a1f`

Validação local:

- 25 arquivos PHP: lint PASS;
- JS syntax PASS;
- ZIP integrity PASS;
- staging↔ZIP parity PASS;
- mudança runtime limitada ao bootstrap de versão e ao runner diagnóstico temporário;
- Knowledge Document `2.0.1`, Legacy adapter, Semantic Structure e smoke permanecem inalterados.

Novas métricas agregadas por assinatura:

- `lists_semantic` (DOM bruto);
- `lists_unwrapped_semantic`;
- `lists_parser_emitted`;
- `tables_semantic` (DOM bruto);
- `tables_unwrapped_semantic`;
- `tables_parser_emitted`;
- `shortcodes_recognized`.

O relatório continua sem IDs, títulos, URLs ou conteúdo editorial.

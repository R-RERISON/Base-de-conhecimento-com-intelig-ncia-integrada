# G-530 Closeout — Lexical Engine Local

**Status:** PASS / CLOSED  
**Data:** 2026-09-19  
**Build:** `0.5.0-g530.1`  
**Próximo gate:** G-540 — Corpus / Projection Rebuild

## Implementação

T530:
- `Search_Query_Normalizer`;
- version `search-normalizer-v1.0.0`;
- bounds 256 chars / 1024 bytes / 16 tokens / 128 chars-token.

T531:
- `Search_Document_Builder`;
- `Search_Projection_Repository`;
- schema próprio `bdc_kb_search_documents`;
- criação de schema explicitamente disponível, mas não executada em page load;
- upsert idempotente `WRITTEN|NO_CHANGE`.

T532:
- `Lexical_Ranker`;
- version `lexical-ranker-v1.0.0`;
- pesos/tie-break do contrato G-520;
- false positive por substring rejeitado por cobertura lexical.

T533:
- `Search_Service`;
- result version `search-result-v1.0.0`;
- autorização WordPress revalidada antes do ranking;
- matched_signals explicáveis.

T534:
- Projection não ready/error => `wordpress_fallback`;
- state `degraded`;
- score=null;
- native relevance sem `modified DESC`.

## Candidate retrieval

- strict all-token;
- relaxed any-token somente para completar pool;
- cap 200;
- ordem SQL `post_id ASC`;
- token boundary via `CONCAT(' ', field, ' ') LIKE '% token %'`;
- ranker repete validação lexical como defesa em profundidade.

## Segurança

Zero:
- write editorial;
- network call;
- query logging;
- ASI runtime/storage;
- FULLTEXT;
- semantic/vector/AI;
- schema/rebuild implícito no bootstrap.

Writes existentes são somente derivados e autorizados:
- state Option;
- Search Projection row.

## Testes

Evidence:
`evidence/g530-local-validation-20260919.json`

- 17/17 unit/integration PASS;
- 7/7 PHP lint;
- local/GitHub blob parity 7/7.

Defeitos capturados durante revisão:
1. substring LIKE podendo gerar candidato sem token lexical real;
2. rewrite desnecessário de row idêntica.

Ambos possuem regressão automatizada.

## T537

**G-530 = PASS/CLOSED.**

Isso autoriza G-540 para:
- criar schema explicitamente em homologação;
- rebuild full-corpus;
- duas passagens;
- determinismo;
- idempotência;
- coverage;
- zero fatal/throwable.

Ainda não autoriza UI final, Golden gate, FULLTEXT, produção ou merge.
